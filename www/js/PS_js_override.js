// PS_scroll_to

function PS_sc2_expand(in_id, in_offset) {
    // this file is called from html param >> ps_cc='' // from html

    $('#' + in_id + ' div').removeClass('hide');

    $('#' + in_id + '_btn').addClass('hide');

};
